#include<stdio.h>
void main()
{
    int i, n, sum=0, avg;
    for ( i = 1; i <=10; i++)
    {
         printf("Enter the number");
        scanf("%d\n",n);
        sum=sum+n;
    }
    avg=sum/10;
    printf("sum is %d",sum);
    printf("average is %d",avg);
    
}